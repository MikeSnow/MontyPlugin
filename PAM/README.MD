#Pluggable Authentication Modules

This module is a api , we do this api in python and make with c.
This module is a facial recognition more rfid token 
The token is new level security, in this case the rfid detect transport pass

For the use this module is important copy to /lib/security and modify /etc/pam.cnf